# 1971–1972 K10 Rocker two-tone

September 24, 2026. Nick requested a **Rocker** option alongside Center band, starting with the paired 1971–1972 K10. Secondary paint begins below the bottom edge of the lower body molding. Upper panels remain the main body color, and the existing contrasting cab option stays independent.

Four new `rocker-mask.png` files live under `public/designer/studio/chevrolet-k10-1972-color-v4/{view}/`. Native polygons follow the lower trim in each view and are intersected with the existing body mask. This includes the narrow painted tailgate strip below the applique and above the rear bumper. Straight-front coverage is intentionally empty because the bumper hides the lower body panels. Chrome, wheels, tires, glass and background are excluded.

Only this group's studio packs advertise `rockerPaint`. Saved drafts, shared links, exports and quote reviews retain Rocker; legacy individual 1971/1972 shared IDs still normalize to the paired group. Other vehicles keep their existing pattern choices. Center band remains the default, and selecting Solid ignores the secondary paint layer.

The native build, verification and four-view previews are in workspace `assets/k10-1971-1972-rocker/`. All 24 pre-existing layer files remain byte-identical. An independent visual review checks blue/white and white/red contrast treatments, plus enlarged trim details. Reconstructed pre-feature regression checks preserve all 33 defaults and summaries and 264 default/Center-band SVG outputs.

Publishing was explicitly authorized by Nick during this task. Included in the [September 24 designer release](designer-release-2026-09-24.md), covering both the website and quote Worker.

## Rear-quarter cab and Rocker edge cleanup

Nick requested cleaner rear-quarter cab and Rocker paint edges, while retaining the selected secondary color on the Rocker panels. This follow-up uses `chevrolet-k10-1972-color-v5/rear-quarter` only for the paired 1971–1972 K10 rear view. The pack starts from the seven v4 rear-quarter layers; the native refinement updates the affected paint masks and texture. The other three views keep their v4 roots, and all alternative wheel scenes keep their existing sources.

Center band remains the default. Saved/shared individual 1971 and 1972 IDs still resolve to the paired group; both paint patterns, independent cab contrast, selected wheels and secondary paint continue through the shared renderer and private quote views. Existing export coverage now checks both patterns across all wheel/view combinations, including all seven v5 rear-layer files. Quote coverage checks the revised rear root alongside the unchanged other views for stock and Baja Black wheels.

The rear cab boundary follows the actual pillar/window bases instead of cutting across them. Rocker paint meets the lower molding without thin body-color gaps. A 427-pixel painted fender corner previously excluded as part of a wheel is restored from the original blue source; all four alternative wheel scenes have identical pixels in that region. The tailgate paint stops at the seam above the bumper. A native SVG clip desaturates only the original bumper-lip reflection, preserving chrome shading without repainting it.

Native geometry, sources and checks are in workspace `assets/k10-1971-1972-rear-cleanup/` and `assets/k10-1971-1972-rear-cab-cleanup/`. The studio photograph and all original v4 assets remain unchanged. The cab correction changes 409 mask pixels. All 45 repository tests, TypeScript and focused lint pass; browser review confirms the combined cab, Rocker and protected chrome treatment.
